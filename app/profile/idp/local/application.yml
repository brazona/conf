server:
  port: 7782
  servlet:
    context-path: /api
spring:
  profiles:
    active: local
  application:
    name: idp
  jpa:
    properties:
      hibernate:
        default_schema: idp
        jdbc:
          lob:
            non_contextual_creation: 'true'
        temp:
          use_jdbc_metadata_defaults: 'false'
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:postgresql://localhost:5454/bza
    password: Z4HKTFW5WY
    username: idp
    schema: idp
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: '20000'
      idle-timeout: '10000'
      maximum-pool-size: '40'
      auto-commit: 'true'
      minimum-idle: '10'

jwt:
  secret: RXN0aGVyemluaGFBbW9yRGFNaW5oYVZpZGE=
  expiration_ms: 86000000
  auth:
    converter:
      resource-id: idp
      principal-attribute: principal_username

keycloak:
  url: http://localhost:8084/realms/brazona
  name: idp
  grant: password
  client:
    id: idp
    secret: BNUGEtF3P62q1FQpyIHMMDCw6mtTedVN
 
logging:
  path: ./src/main/resources/log
  file:
    path: ./src/main/resources/log
    name: ./src/main/resources/log/idp.log
    max-size: 10MB
    max-history: 10
    pattern:
      console: '%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n'

  #level: ERROR, WARN, INFO, DEBUG E TRACE    
  level:
    root: WARN
    org:
      hibernate: WARN
      springframework:
        security: WARN
        data: WARN
        web: WARN
    br:
      brazona:
        idp:
          api: TRACE  
idp:
  security:
    basic:
      username: LOCAL**O60ZS6G6
      password: PfQuy2Ml

mail:
  smtp:
    host: smtp.gmail.com
    port: 587
    auth: 'true'
    from: cezarfelipe2008@gmail.com
    ssl:
      trust: smtp.gmail.com
      host: smtp.gmail.com
      factory_port: 465
      port: 465
      class: javax.net.ssl.SSLSocketFactory
    starttls:
       enable: true
       required: true
    credential:
      username: cezarfelipe2008@gmail.com"
      password: fnnx ubyy curd zrti

      